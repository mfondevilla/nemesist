{% extends 'base.html.twig' %}

{% block title %}Busqueda de items por catálogo{% endblock %}

{% block body %}
{% block content %}
<div class="example-wrapper">
  <h2>
        {% if edit is defined %}
            Editar item 
        {% else %}
            Crear itema {{ catalogue.title }}
        {% endif %}
        
    </h2>
        
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}
{% endblock %}