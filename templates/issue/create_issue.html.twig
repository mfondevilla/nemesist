{% extends 'base.html.twig' %}

{% block title %}Busqueda de números por revista{% endblock %}

{% block body %}
{% block content %}
    
 <main class="principal principal-xs container">
        <div class="row alto">
        {{ include('includes/subnav.html.twig') }}
        
       <div class="col-8 col-8">
  <h2>
        {% if edit is defined %}
            Editar número 
        {% else %}
            Crear número {{ catalogue.title }}
        {% endif %}
        
    </h2>
        
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
       </div>
        </div>
    </main>
</div
{% endblock %}
{% endblock %}